{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install requirements",
      "type": "shell",
      "command": "${config:python.defaultInterpreterPath} -m pip install -U pip -r requirements.txt",
      "problemMatcher": []
    }
  ]
}
